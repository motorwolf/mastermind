
import { createUseStyles } from 'react-jss';

export const ColorRowStyles = createUseStyles((theme: any) => ({
    colorRow: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'nowrap',
        width: '75%',
    },
}));
